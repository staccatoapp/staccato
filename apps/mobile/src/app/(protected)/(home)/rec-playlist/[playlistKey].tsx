import React from "react";

import { PlaylistScreen } from "@/components/playlist/playlist-screen";

/** Recommended playlist detail pushed within the Home tab stack. */
export default function HomeRecPlaylistScreen() {
  return <PlaylistScreen mode="recommended" />;
}
