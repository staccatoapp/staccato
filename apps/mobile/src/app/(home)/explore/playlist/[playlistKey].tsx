import React from "react";

import { PlaylistScreen } from "@/components/playlist/playlist-screen";

/** Recommended playlist detail pushed within the Explore tab stack. */
export default function ExplorePlaylistScreen() {
  return <PlaylistScreen mode="recommended" />;
}
