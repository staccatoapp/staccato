/**
 * Decorative icon-tile colors for settings rows (iOS app-icon style squares).
 * These are presentational accents, not semantic theme tokens, so they live
 * here rather than in the theme. Where an in-app token matches closely it is
 * reused (orange = primary, blue = serverBlue, green = success).
 */
export const TILE = {
  blue: "#1a83db",
  teal: "#2a9bb5",
  green: "#54bf5c",
  orange: "#fd7933",
  purple: "#8b5cf6",
  pink: "#db4a8a",
  gray: "#6b6b6b",
  red: "#d8504f",
} as const;
